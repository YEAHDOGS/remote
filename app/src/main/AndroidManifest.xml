<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to drive the IR blaster. Normal permission: granted at install. -->
    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <!-- IR is the whole point of the app, but don't hard-require it so the
         app still installs on devices without an emitter (it shows a notice). -->
    <uses-feature android:name="android.hardware.consumerir" android:required="false" />

    <application
        android:label="DOGS Remote"
        android:theme="@style/Theme.DogsRemote"
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
